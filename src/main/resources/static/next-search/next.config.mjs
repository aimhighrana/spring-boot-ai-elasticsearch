/** @type {import('next').NextConfig} */
const nextConfig = {
    // distDir: 'build',
    cleanDistDir: true,
    // basePath: '/build',
    productionBrowserSourceMaps: true
};


export default nextConfig;
